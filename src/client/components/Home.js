import React from 'react';

const Home = () => {
  return (
    <div>
      <h3>Home component</h3>
      <button onClick={() => console.log('clicked')}>Click Me</button>
    </div>
  );
};

export default Home;
